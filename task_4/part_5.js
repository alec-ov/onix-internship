const task = "Given object {js: 'test', jq: 'hello', css: 'world'}. Get an array of its keys, and an array of its value";

console.log("\n  4.5*:", task);

const obj = {js: "test", jq: "hello", css: "world"};

console.log(Object.keys(obj), Object.values(obj));